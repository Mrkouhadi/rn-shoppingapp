export default class CartItems {
    constructor(quantity, productTitle, productPrice, sum){
        this.quantity = quantity;
        this.productTitle = productTitle;
        this.productPrice = productPrice;
        this.sum = sum;
    }
}